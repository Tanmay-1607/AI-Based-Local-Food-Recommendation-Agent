# Frontend Environment Variables (Optional for Render)
# If deploying frontend as a separate Render Static Site, set:
# VITE_API_URL=https://your-backend-name.onrender.com
# For unified single-service Render deployment, this is NOT needed (defaults to /api)
